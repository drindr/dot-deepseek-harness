# Local-only plugin (machine-specific patch, never pushed)
tailscale-patch/

# Local node_modules symlinks for link:-installed plugins (machine-specific)
flash-device-auth/node_modules/

# Runtime / tooling state
.chrome-mobile/
.pnpm-cache/
.pnpm-store/
.runtime/

# Scratch / debug artifacts (machine-local, not committed)
.*.mjs
.chrome-tmp/
.debug/
.npm-cache/
.tmp/

# CA material (self-signed certs/keys) — never commit
*.crt
*.cer
*.pem
*.key
*.p12
*.pfx
pki/
authorities/
